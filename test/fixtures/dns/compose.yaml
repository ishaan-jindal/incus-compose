services:
  service-db:
    image: docker.io/alpine:edge
    container_name: my-db
    x-incus:
      limits.cpu: 1
    networks:
      default:
        aliases:
          - db.mydomain.lan

  dev:
    image: docker.io/alpine:edge

# networks:
#   default:
#     x-incus:
#       # The target must be an incus instance name, see: https://serverfault.com/a/947713
#       raw.dnsmasq: |
#         cname=db.mydomain.lan,my-db
