services:
  app:
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug
      HOT_RELOAD: "true"

    command: npm run dev

  db:
    environment:
      POSTGRES_PASSWORD: devpassword

  adminer:
    image: docker.io/library/adminer:latest
    depends_on:
      - db
