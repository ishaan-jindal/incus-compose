services:
  web:
    image: docker.io/nginx:alpine
    ports:
      - "8080:80"
    x-incus:
      limits.memory: 512MB
      limits.cpu: "2"
      security.nesting: "false"

  database:
    image: docker.io/nginx:alpine
    x-incus:
      limits.memory: 1GB
      limits.cpu: "4"
