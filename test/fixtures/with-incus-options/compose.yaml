services:
  web:
    image: docker.io/alpine:edge
    dns:
      - 8.8.8.8
    x-incus:
      limits.memory: 1024MB
      limits.cpu: "2"
      security.nesting: "false"
      oci.dns.nameservers: "9.9.9.9"

  database:
    image: docker.io/alpine:edge
    x-incus:
      limits.memory: 1GB
      limits.cpu: "4"
