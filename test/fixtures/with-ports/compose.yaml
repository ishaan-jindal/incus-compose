services:
  web:
    image: docker.io/caddy:latest
    ports:
      - "8080:80"
    x-incus:
      limits.cpu: 1

  web-nat:
    image: docker.io/alpine:edge
    ports:
      - published: "8081"
        target: "80"
        x-incus-compose:
          nat: true
    x-incus:
      limits.cpu: 1
