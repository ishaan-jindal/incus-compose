services:
  web1:
    image: docker.io/alpine:edge
    restart: unless-stopped
    x-incus:
      limits.cpu: 1

  web2:
    image: docker.io/alpine:edge
    restart: unless-stopped
    x-incus:
      limits.cpu: 1
