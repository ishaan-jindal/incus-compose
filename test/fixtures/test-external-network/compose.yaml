services:
  web:
    image: docker.io/nginx:alpine
    environment:
      NGINX_ENTRYPOINT_WORKER_PROCESSES_AUTOTUNE: 1
    x-incus:
      limits.cpu: 1
    networks:
      - br0

networks:
  br0:
    external: true
    x-incus-compose:
      network: incusbr0
