services:
  service-db2:
    image: docker.io/alpine:edge
    container_name: my-db2
    x-incus:
      limits.cpu: 1
    networks:
      default:
        aliases:
          - db2.mydomain.lan

networks:
  default:
    external: true
    x-incus-compose:
      network: dns-default
