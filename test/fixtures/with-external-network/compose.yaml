name: with-external-network

services:
  web:
    image: docker.io/nginx:alpine
    networks:
      - named-override

networks:
  named-override:
    external: true
    x-incus-compose:
      network: incusbr0
