name: with-external-network

services:
  web:
    image: docker.io/alpine:edge
    x-incus:
      limits.cpu: 1
    networks:
      - named-override

networks:
  named-override:
    external: true
    x-incus-compose:
      network: incusbr0
