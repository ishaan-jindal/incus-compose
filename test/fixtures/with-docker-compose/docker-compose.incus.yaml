services:
  web:
    ports: !reset []
    x-incus:
      limits.cpu: 1
    networks:
      default:
        ipv4_address: 10.131.35.17

networks:
  default:
    x-incus:
      ipv4.nat: "true"
      ipv4.address: 10.131.35.1/24
