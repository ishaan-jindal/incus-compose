x-incus-compose:
  healthd:
    incus: https://10.0.0.1:8443
    network: healthd:default
    external: true
    scope: project
    workers: 64
    restart-workers: 8
    x-incus:
      limits.cpu: 4
      limits.memory: 512MB

services:
  web:
    image: docker.io/alpine:edge
