name: with-docker-compose
services:
  web:
    image: docker.io/alpine:edge
    networks:
      default:
        ipv4_address: 10.131.35.17
    restart: unless-stopped
    x-incus:
      limits.cpu: 1
networks:
  default:
    name: with-docker-compose_default
    x-incus:
      ipv4.address: 10.131.35.1/24
      ipv4.nat: "true"
