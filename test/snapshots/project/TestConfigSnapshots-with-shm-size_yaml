name: with-shm-size
services:
  app:
    image: docker.io/alpine:edge
    networks:
      default: null
    shm_size: "134217728"
    x-incus:
      limits.cpu: 1
networks:
  default:
    name: with-shm-size_default
