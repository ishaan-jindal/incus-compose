name: proxy
services:
  backend1:
    entrypoint:
      - /bin/sh
      - -c
      - mkdir -p /www && echo backend1-ok > /www/index.html && httpd -f -v -p 8080
        -h /www
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - --spider
        - http://127.0.0.1:8080
      timeout: 5s
      interval: 5s
      retries: 3
    image: docker.io/library/busybox:glibc
    networks:
      default: null
    restart: unless-stopped
    x-incus:
      limits.cpu: 1
  backend2:
    entrypoint:
      - /bin/sh
      - -c
      - mkdir -p /www && echo backend2-ok > /www/index.html && httpd -f -v -p 8080
        -h /www
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - --spider
        - http://127.0.0.1:8080
      timeout: 5s
      interval: 5s
      retries: 3
    image: docker.io/library/busybox:glibc
    networks:
      default: null
    restart: unless-stopped
    x-incus:
      limits.cpu: 1
  frontend:
    depends_on:
      backend1:
        condition: service_healthy
        required: true
      backend2:
        condition: service_healthy
        required: true
    entrypoint:
      - /bin/sh
      - -c
      - mkdir -p /www && echo frontend-ok > /www/index.html && httpd -f -v -p 8080
        -h /www
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - --spider
        - http://127.0.0.1:8080
      timeout: 5s
      interval: 5s
      retries: 3
    image: docker.io/library/busybox:glibc
    networks:
      default: null
    restart: unless-stopped
    x-incus:
      limits.cpu: 1
networks:
  default:
    name: proxy_default
