Read .rules and .rules.local.md, make sure you have CONTRIBUTING.md and `.claude/settings*.json` as your rules loaded.
Then preload all docs as in `docs/**.md`

Then execute issue #$ARGUMENTS without plan mode.
