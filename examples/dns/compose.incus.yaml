services:
  dnscrypt-proxy:
    networks:
      default:
        ipv4_address: "${DNSCRYPT_IPV4_ADDRESS}/${IPV4_NETMASK}"

  knot:
    volumes: !reset
      - ./knot/config:/config:ro
      - type: bind
        source: ./knot/storage
        target: /storage
        x-incus:
          security.shifted: "true"
          initial.uid: "53"
          initial.gid: "53"
    networks:
      default:
        ipv4_address: "${KNOT_IPV4_ADDRESS}/${IPV4_NETMASK}"

networks:
  default:
    external: true
    x-incus-compose:
      network: ${EXTERNAL_NETWORK}
